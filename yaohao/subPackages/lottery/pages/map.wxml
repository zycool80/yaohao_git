<view style="width:100%;height:{{winHeight}}px;">
    <map controls="{{controls}}" id="map" latitude="{{lat}}" longitude="{{long}}" markers="{{markers}}" polyline="{{polyline}}" scale="13" showLocation="show-location" style="width: 100%; height: 100%;"></map>
</view>
